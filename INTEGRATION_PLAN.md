# 系统架构与集成规划

## 🏗️ 系统分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    应用程序层 (Application)                      │
│                unified_system.py - 集成核心                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 统一界面 | 决策引擎 | 配置管理 | 日志系统 | 性能监控      │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   算法处理层 (Algorithm)                         │
├──────────────────────┬──────────────────────┬──────────────────┤
│   YOLO目标检测       │  SLAM障碍物分析     │  点云处理         │
│ person_detect.py    │ depth_slam_          │ mid70_vis.py     │
│                     │ obstacle.py          │                  │
└──────────────────────┴──────────────────────┴──────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   数据处理层 (Data Processing)                   │
├──────────────────────┬──────────────────────┬──────────────────┤
│  图像预处理          │  深度处理             │  点云处理         │
│  ├─ 格式转换        │  ├─ 滤波             │  ├─ 坐标转换    │
│  ├─ 缩放            │  ├─ 无效值处理       │  ├─ 滤波         │
│  └─ 归一化          │  └─ 平滑             │  └─ 可视化       │
└──────────────────────┴──────────────────────┴──────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   驱动与接口层 (Driver Layer)                    │
├──────────────────────┬──────────────────────┬──────────────────┤
│  Berxel相机驱动      │  Mid-70雷达驱动     │  通信接口        │
│ berxel_camera.py    │ ROS2接口             │ UDP/TCP          │
│ ↓                   │ /livox/lidar         │                  │
│ berxel_wrapper.cpp  │                      │                  │
└──────────────────────┴──────────────────────┴──────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   硬件层 (Hardware)                              │
├──────────────────────┬──────────────────────┬──────────────────┤
│  Berxel P100R        │  Livox Mid-70        │  计算平台        │
│  RGB-D深度相机       │  线性激光雷达        │  • AMD 780M      │
│  分辨率: 640×480     │  扫描频率: 10Hz      │  • 内存: 16GB    │
│  帧率: 30 FPS        │  距离: 0.1-200m      │  • GPU: ROCm     │
└──────────────────────┴──────────────────────┴──────────────────┘
```

---

## 📊 数据流向图

```
┌──────────────┐
│ Berxel相机   │  RGB帧        ┌──────────────────┐
│ RGB-D传感器  ├─────────────→│ 图像预处理      │
│              │  深度图        └─────────┬────────┘
└──────────────┘                         │
                                         ├─────────────────→ [缓存]
                                         ↓
                                    ┌─────────────┐
                                    │ YOLO检测    │
                                    │ 416×416推理 │  目标框
                                    └──────┬──────┘
                                           │
┌──────────────┐   点云                    │    ┌──────────────────┐
│ Mid-70雷达   ├─────────────→ 点云处理 ──┼───→│  融合决策层      │
│ 激光雷达     │   ROS2话题     & 可视化   │    │ decision_maker   │
└──────────────┘                         │    └────────┬─────────┘
                                         │             │
                                    ┌────┴─────────┐   │
                                    │ SLAM处理     │   │
                                    │ 障碍物检测   │   │
                                    └──────┬───────┘   │
                                           │           │
                                     [缓存]←┘           │
                                           │           │
                                     ┌─────v───────────v─────┐
                                     │   可视化显示          │
                                     │ ├─ 目标框            │
                                     │ ├─ 障碍物区域        │
                                     │ ├─ 导航方向箭头      │
                                     │ ├─ FPS/性能指标      │
                                     │ └─ 点云3D视图        │
                                     └──────────────────────┘
                                           │
                                           ├─→ 导航命令输出
                                           ├─→ 日志记录
                                           └─→ 性能统计
```

---

## 🔄 实时处理流程

### 单帧处理时序图

```
时间线(ms)  0    5    10   15   20   25   30   33
           │    │    │    │    │    │    │    │
           ├────┤                            ← 帧获取 (5ms)
                ├────┤                        ← 图像预处理 (5ms)
                     ├────────┤                ← YOLO推理 (15-20ms @ 416)
                               ├──┤            ← 后处理 (3ms)
                     ├────┤                   ← 深度处理 (5ms, 每3帧1次)
                          ├────────┤          ← SLAM分析 (10-15ms)
                                   ├──┤       ← 融合决策 (2ms)
                                      ├───┤   ← 可视化渲染 (5ms)
           ├─────────────────────────────┤    ← 显示更新 (30ms = 33FPS)
```

**关键路径**: RGB获取 → YOLO推理 → 显示 = ~25ms
**并行处理**: 深度处理独立进行，不阻塞主流程

---

## 🎯 当前代码现状分析

### ✅ 已完成模块

| 模块 | 文件 | 状态 | 完成度 |
|------|------|------|--------|
| RGB-D驱动 | berxel_camera.py | ✅ | 100% |
| YOLO检测 | person_detect.py | ✅ | 100% |
| SLAM分析 | depth_slam_obstacle.py | ✅ | 100% |
| 整合原型 | person_detect_slam.py | ✅ | 90% |
| 激光雷达可视化 | mid70_vis.py | ✅ | 100% |
| ROS2接口 | ros2_ws/ | ✅ | 80% |

### ⚠️ 待完善模块

| 模块 | 优先级 | 工作量 | 备注 |
|------|--------|--------|------|
| 统一可视化界面 | 🔴 高 | 中等 | 需完全重构 |
| 融合决策器 | 🔴 高 | 中等 | 逻辑算法设计 |
| 配置管理系统 | 🟡 中 | 小 | 参数外部化 |
| 性能监控 | 🟡 中 | 小 | 实时性能显示 |
| 完整日志系统 | 🟢 低 | 小 | 调试追踪用 |

---

## 🚀 整合版本总体设计

### 核心文件结构

```python
# unified_system.py (新建 - 主程序)
class UnifiedRobotSystem:
    def __init__(self, config_file='config.yaml'):
        self.config = load_config(config_file)
        self.camera = self._init_camera()
        self.detector = self._init_detector()
        self.slam = self._init_slam()
        self.visualizer = self._init_visualizer()
        self.logger = setup_logger()
        self.perf_monitor = PerformanceMonitor()
    
    def run(self):
        """主循环 - 核心处理流程"""
        while self.running:
            # 1. 获取传感器数据
            rgb, depth = self.camera.get_frame_and_depth()
            
            # 2. 并行处理 (多线程或异步)
            person_dets = self.detector.detect(rgb)
            obstacle_info = self.slam.process(depth, rgb)
            
            # 3. 融合决策
            nav_decision = self.fuse_decisions(person_dets, obstacle_info)
            
            # 4. 输出与显示
            self.visualizer.render(rgb, person_dets, obstacle_info, nav_decision)
            self.output_commands(nav_decision)
            
            # 5. 监控与记录
            self.perf_monitor.update(time_frame)
```

### 配置管理

```yaml
# config.yaml (新建)
camera:
  type: "berxel"
  width: 640
  height: 480
  fps: 30
  
detection:
  model: "yolo12n.onnx"
  input_size: 416
  conf_threshold: 0.40
  backend: "onnx"  # onnx | pytorch
  
slam:
  depth_near: 0.5
  depth_far: 5.0
  min_navigable_area: 1000
  
performance:
  target_fps: 30
  skip_frames: 1
  display_scale: 0.5
  max_detections: 100
```

---

## 📈 性能优化策略

### 阶段1: 基线建立 (当前)
- ✅ YOLO ONNX化 (416×416)
- ✅ 深度SLAM优化
- ✅ 帧跳跃策略
- **当前性能**: 25-28 FPS

### 阶段2: 算法优化 (下一步)
- [ ] 多线程并行处理
- [ ] 结果缓存与插值
- [ ] 动态分辨率调整
- **目标性能**: 28-30 FPS

### 阶段3: 系统优化
- [ ] SLAM GPU加速 (如可用)
- [ ] 点云实时处理
- [ ] 内存优化
- **目标性能**: 30+ FPS

### 阶段4: 部署优化
- [ ] 模型量化 (INT8/FP16)
- [ ] 边缘设备适配
- [ ] 网络优化
- **目标**: 移动端部署

---

## 🔧 集成检查清单

### 前置条件
- [ ] 编译C++ Wrapper (berxel_wrapper.so)
- [ ] 验证Berxel相机通信
- [ ] 验证Mid-70激光雷达通信
- [ ] ONNX模型可用

### 代码集成
- [ ] 创建 `src/` 目录结构
- [ ] 创建 `unified_system.py`
- [ ] 创建 `decision_maker.py`
- [ ] 创建 `unified_visualizer.py`
- [ ] 创建配置管理系统

### 测试验证
- [ ] 单元测试 (各模块独立)
- [ ] 集成测试 (模块间通信)
- [ ] 性能测试 (FPS/延迟)
- [ ] 压力测试 (长时间运行)
- [ ] 故障恢复测试

### 文档完成
- [ ] API文档
- [ ] 使用手册
- [ ] 调试指南
- [ ] 部署指南

---

## 📝 下一步行动

### 立即执行 (本周)
1. **清理代码结构**
   ```bash
   mkdir -p src/{core,sensors,detection,slam,visualization}
   mkdir -p tests docs
   ```

2. **创建模块框架**
   - `unified_system.py` (主程序骨架)
   - `decision_maker.py` (决策逻辑)
   - `config_manager.py` (配置加载)

3. **统一参数配置**
   - 编写 `config.yaml`
   - 所有硬编码参数统一导入

### 短期计划 (1-2周)
4. **完善可视化**
   - 整合多窗口显示
   - 性能指标实时显示
   - 交互控制界面

5. **融合决策逻辑**
   - 目标与障碍物冲突解决
   - 优先级权重设定
   - 输出命令格式规范

### 中期计划 (2-4周)
6. **ROS2集成**
   - 标准化发布/订阅接口
   - 与Nav2导航栈集成
   - 性能基准测试

7. **测试与优化**
   - 完整测试套件
   - 性能瓶颈分析
   - 针对AMD 780M的特殊优化

---

## 💡 设计建议

### 可扩展性设计
- 使用配置文件，避免硬编码
- 模块化架构，支持插件式扩展
- 清晰的接口定义，便于模块替换

### 性能设计
- 异步I/O与多线程
- 智能帧跳跃与动态调整
- 结果缓存与插值

### 鲁棒性设计
- 异常处理与降级机制
- 健康检查与自动恢复
- 详细的日志与诊断

### 易用性设计
- 一键启动脚本
- 可视化配置工具
- 实时性能监控仪表板

---

**文档生成时间**: 2026-01-18  
**版本**: 1.0  
**维护**: Zhuo RM Team
